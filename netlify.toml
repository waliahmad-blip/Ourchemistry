# Netlify configuration for Next.js (App Router)
# The @netlify/plugin-nextjs plugin is auto-installed by Netlify when it
# detects a Next.js site; declaring it here makes the setup explicit.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
